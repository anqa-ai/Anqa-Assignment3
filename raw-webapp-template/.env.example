# BFF Proxy Configuration
# These are set as ECS task secrets (per client deployment)
# Never exposed to browser - only used by Next.js API routes

CLIENT_UUID=your-client-uuid-here
CLIENT_API_KEY=your-client-api-key-here


# Cognito EMAIL_OTP Authentication
COGNITO_EXTERNAL_CLIENT_ID=6urk7cliabnfaprdrpbputp6a9
AWS_REGION=eu-west-2

# Optional: Override API URL (defaults to https://dev.api.anqa.ai)
# NEXT_PUBLIC_API_URL=https://dev.api.anqa.ai

# Note: Each client runs in a dedicated ECS task with their own secrets
# BFF proxy (/api-proxy) reads these and adds credentials to API requests
